Element:
Conditionals: a feature of a programming language, which perform different computations or actions depending on whether a programmer-specified boolean condition evaluates to true or false.

Syntax:
if condition = true:
    print(true)

How to Use It: Use it to choose to do one action or another, also useful for checking and looping.

Example: An if then statement.
